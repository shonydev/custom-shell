{
  "_comment": "Not wired up yet - placeholder for when config grows past a couple of flags. For now, mode is chosen via --dev-overlay / --full-shell command-line args in App.xaml.cs.",
  "bar": {
    "height": 32,
    "edge": "Top"
  },
  "modules": {
    "taskbar": { "enabled": true },
    "tray": { "enabled": true },
    "systemControls": { "enabled": true },
    "startMenu": { "enabled": true }
  }
}
